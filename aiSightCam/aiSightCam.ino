#include <WiFi.h>
#include <WiFiClient.h>
#include <ESPAsyncWebServer.h>
#include "esp_camera.h"

// Network Credentials
const char* ssid = "ESP32-CAM";
const char* password = "aVeryStrongPassword";

// Create an access point

void setup() {
    
}

void loop() {
    delay(100);`
}